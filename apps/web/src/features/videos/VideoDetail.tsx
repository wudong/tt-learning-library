import { useVideo } from '../../lib/api/hooks'
import { VideoThumbnail } from '../../components/VideoThumbnail'
import { YouTubeEmbed } from '../../components/YouTubeEmbed'

export function VideoDetail({ id }: { id:string }) { const video=useVideo(id); const data=video.data; return <section><h1>{data?.video.title || 'Video'}</h1>{video.isLoading&&<div className="card">Loading…</div>}{data&&<><div className="card">{data.video.sourcePlatform === 'youtube' && data.video.externalId ? <YouTubeEmbed externalId={data.video.externalId} title={data.video.title || data.node.title}/> : <VideoThumbnail src={data.video.thumbnailUrl} title={data.video.title || data.node.title}/>}<span className="pill">{data.video.sourcePlatform}</span><h2>{data.node.title}</h2>{data.video.creatorName&&<p className="muted">By {data.video.creatorName}</p>}<p className="muted">{data.video.progress} • {data.video.learningState}</p><a className="button" href={data.video.sourceUrl} target="_blank" rel="noreferrer">Open Video</a></div><div className="card"><h3>Quick actions</h3><div className="quick-actions"><button className="button secondary">Note</button><button className="button secondary">Timestamp</button><button className="button secondary">Drill</button><button className="button secondary">More</button></div></div><div className="card"><h3>Learning context</h3>{data.topics.length===0&&data.skills.length===0?<p className="muted">No topics or skills linked yet. Organize this video from the Library.</p>:<><div className="mini-chips">{data.topics.map(topic=><span key={topic.id}>{topic.title}</span>)}</div>{data.skills.map(skill=><p key={skill.id}><strong>{data.skillRelationships[skill.id] === 'demonstrates' ? 'Demonstrates' : 'Explains'}:</strong> {skill.title}</p>)}</>}</div><div className="card"><h3>Related Items</h3>{data.related.length===0?<p className="muted">No other related items yet.</p>:data.related.map(n=><p key={n.id}>{n.title}</p>)}</div></>}</section> }
