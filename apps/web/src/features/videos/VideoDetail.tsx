import { useVideo } from '../../lib/api/hooks'
export function VideoDetail({ id }: { id:string }) { const video=useVideo(id); const data=video.data; return <section><h1>{data?.video.title || 'Video'}</h1>{video.isLoading&&<div className="card">Loading…</div>}{data&&<><div className="card"><span className="pill">{data.video.sourcePlatform}</span><h2>{data.node.title}</h2><p className="muted">{data.video.progress} • {data.video.learningState}</p><a className="button" href={data.video.sourceUrl} target="_blank" rel="noreferrer">Open Video</a></div><div className="card"><h3>Quick actions</h3><div className="quick-actions"><button className="button secondary">Note</button><button className="button secondary">Timestamp</button><button className="button secondary">Drill</button><button className="button secondary">More</button></div></div><div className="card"><h3>Related Items</h3>{data.related.length===0?<p className="muted">No related graph items yet.</p>:data.related.map(n=><p key={n.id}>{n.title}</p>)}</div></>}</section> }
