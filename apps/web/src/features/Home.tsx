import { useInbox, useVideos } from '../lib/api/hooks'
export function Home({ navigate }: { navigate: (to:string)=>void }) {
  const inbox = useInbox(); const videos = useVideos()
  return <section><h1>Keep your table tennis learning moving</h1><p className="muted">Save tutorials quickly, organize them by skill, then turn ideas into notes and drills.</p><div className="card"><h2>Quick Add</h2><p className="muted">Paste a video URL, or install the PWA and share supported links directly into your Inbox.</p><div className="quick-actions"><button className="button" onClick={()=>navigate('/videos/new')}>Add Video</button><button className="button secondary" onClick={()=>navigate('/inbox')}>Open Inbox</button></div></div><h2 className="section-title">Continue Learning</h2><div className="card"><strong>{videos.data?.[0]?.title ?? 'Start with a saved tutorial'}</strong><p className="muted">{videos.data?.length ? 'Recently saved video ready to revisit.' : 'No videos yet. Save a table tennis tutorial to begin.'}</p></div><h2 className="section-title">Inbox</h2><div className="card row"><span>{inbox.data?.length ?? 0} captures waiting</span><button className="button secondary" onClick={()=>navigate('/inbox')}>Review</button></div></section>
}
